import Stack from "@mui/material/Stack";

import ButtonCalculadora from "../Button";

const Calculadora = () => {
  return (
    <Stack spacing={2}>
      <ButtonCalculadora />
    </Stack>
  );
};

export default Calculadora;
